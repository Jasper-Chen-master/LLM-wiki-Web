import { createContext, type ChangeEvent, type FormEvent, useCallback, useContext, useEffect, useMemo, useRef, useState } from "react";
import { Link, Navigate, NavLink, Route, Routes, useNavigate, useParams } from "react-router-dom";
import type { Project, ProjectSnapshot, WikiNode, WikiProfile } from "../shared/contracts";
import { api } from "./api";

type Language = "en" | "zh";
const copy = {
  en: { delete: "Delete selected", selected: "selected", reprocess: "Process current documents", deleteHelp: "Select one or more uploaded documents, then remove them.", brand: "Evidence Atlas", eyebrow: "GOAL-CONDITIONED RESEARCH WORKSPACE", hero: "Turn papers into a traceable knowledge space.", lede: "Build an evidence-backed research wiki shaped by the questions that matter to you—not a generic document summary.", start: "Start a research project", example: "e.g. Battery cathode degradation", create: "Create project", projects: "Your projects", total: "total", loading: "Loading workspace…", empty: "No projects yet. Create one above to begin a focused research workspace.", active: "Knowledge graph active", review: "Profile ready for review", setup: "Set up research profile", created: "Created", opening: "Opening project…", overview: "Overview", graph: "Knowledge graph", search: "Search", workspace: "Research workspace", workspaceSub: "Define what matters, then build an evidence-bound knowledge graph.", exportJson: "Export JSON", exportCsv: "Export CSV", guide: "Guide the extraction", confirmed: "Confirmed", ready: "Ready for review", required: "Required", help: "Describe the knowledge this wiki should retain. The profile is validated before processing starts.", objective: "Research objective", objectiveHint: "What question should this wiki help answer?", domain: "Research domain", entityTypes: "Important entity types", entitiesHint: "material, process, metric", relations: "Preferred relations", relationsHint: "improves, causes, measured by", ignore: "Information to ignore", ignoreHint: "background, unrelated synthesis", preferences: "Additional preferences", preferencesHint: "Numerical data, units, and evidence are preserved by default.", numeric: "Extract numerical data", units: "Preserve units", evidence: "Require evidence", save: "Save profile", confirm: "Confirm & build graph", rerun: "Run processing again", working: "Working…", upload: "Upload documents", profile: "Research Profile", profileDetail: "DOCX · your goals and priorities", sources: "Source papers", sourceDetail: "PDF or DOCX · up to 20 MB each", choose: "Choose file", chooseMany: "Choose files", noFiles: "No files uploaded yet.", source: "Source document", processing: "Pipeline status", processingHelp: "Confirm the research profile to start parsing, relevance filtering, extraction, entity resolution, and graph construction.", graphSub: "Explore only knowledge retained by your research profile.", searchKnowledge: "Search knowledge", find: "Find a node…", allTypes: "All types", nodes: "nodes", relationsCount: "relations", graphEmpty: "The graph will appear here once profile confirmation and processing complete. Evidence stays attached to every extracted node and relation.", clickNode: "Click a node to inspect its evidence", properties: "Properties", noProperties: "No structured properties.", related: "Related nodes", selectNode: "Select a node.", searchSub: "Search canonical names, aliases, summaries, and structured graph knowledge.", searchHint: "Search entities, properties, or concepts…", matching: "matching nodes", noSummary: "No summary available.", alsoKnown: "Also known as", confidence: "confidence", noResults: "No matching structured knowledge. Try a broader term or process more source documents.", importance: "Importance", language: "Switch language", profileRole: "Research profile", evidenceLabel: "Evidence" },
  zh: { delete: "删除所选文件", selected: "已选择", reprocess: "处理当前文档", deleteHelp: "勾选一个或多个已上传文件后即可删除。", brand: "证据图谱", eyebrow: "目标驱动的研究知识空间", hero: "让论文沉淀为可追溯的知识空间。", lede: "围绕你真正关心的问题，构建以证据为依据的研究 Wiki，而不是泛泛的文档摘要。", start: "创建研究项目", example: "例如：电池正极材料衰减", create: "创建项目", projects: "我的项目", total: "个项目", loading: "正在加载工作空间…", empty: "还没有项目。请在上方创建一个项目，开启目标导向的研究工作流。", active: "知识图谱已就绪", review: "偏好等待审阅", setup: "设置研究偏好", created: "创建于", opening: "正在打开项目…", overview: "概览", graph: "知识图谱", search: "搜索", workspace: "研究工作空间", workspaceSub: "先定义真正重要的信息，再构建有证据支撑的知识图谱。", exportJson: "导出 JSON", exportCsv: "导出 CSV", guide: "设定提取重点", confirmed: "已确认", ready: "待审阅", required: "需要设置", help: "描述此 Wiki 应保留的知识。系统会在开始处理前校验研究偏好。", objective: "研究目标", objectiveHint: "希望这个 Wiki 帮助回答什么问题？", domain: "研究领域", entityTypes: "重要实体类型", entitiesHint: "材料、工艺、指标", relations: "重点关系", relationsHint: "提升、导致、通过…测量", ignore: "需要忽略的信息", ignoreHint: "背景介绍、无关的合成过程", preferences: "其他偏好", preferencesHint: "默认保留数值、单位与证据来源。", numeric: "提取数值数据", units: "保留单位", evidence: "必须保留证据", save: "保存偏好", confirm: "确认并构建图谱", rerun: "再次运行处理", working: "处理中…", upload: "上传文档", profile: "研究偏好", profileDetail: "DOCX · 说明研究目标与关注重点", sources: "源文档", sourceDetail: "PDF 或 DOCX · 单个文件不超过 20 MB", choose: "选择文件", chooseMany: "选择文件", noFiles: "尚未上传文件。", source: "源文档", processing: "处理进度", processingHelp: "确认研究偏好后，系统将依次执行解析、相关性筛选、信息抽取、实体归并与图谱构建。", graphSub: "探索经研究偏好筛选后保留的知识。", searchKnowledge: "搜索知识", find: "查找节点…", allTypes: "全部类型", nodes: "个节点", relationsCount: "条关系", graphEmpty: "确认研究偏好并完成处理后，知识图谱将显示在这里。每个节点和关系都会保留证据来源。", clickNode: "点击节点查看证据", properties: "属性", noProperties: "暂无结构化属性。", related: "相关节点", selectNode: "请选择一个节点。", searchSub: "检索规范名称、别名、摘要与结构化图谱知识。", searchHint: "搜索实体、属性或概念…", matching: "个匹配节点", noSummary: "暂无摘要。", alsoKnown: "别名", confidence: "置信度", noResults: "未找到匹配的结构化知识。请尝试更宽泛的关键词，或处理更多源文档。", importance: "重要度", language: "切换语言", profileRole: "研究偏好", evidenceLabel: "证据" }
} as const;
type Copy = { [K in keyof typeof copy.en]: string };
const I18n = createContext<{ lang: Language; t: Copy; setLang: (lang: Language) => void }>({ lang: "en", t: copy.en, setLang: () => undefined });
const useI18n = () => useContext(I18n);
const emptyProfile: WikiProfile = { version: "1.0", researchGoal: "", domain: "General research", entityTypes: [], importantFields: [], preferredRelations: [], exclude: [], extractNumericData: true, preserveUnits: true, extractTables: false, evidenceRequired: true, notes: "" };
const split = (input: string) => input.split(",").map(value => value.trim()).filter(Boolean);
const join = (input: string[]) => input.join(", ");

export function App() { const [lang, setLang] = useState<Language>(() => localStorage.getItem("llm-wiki-language") === "zh" ? "zh" : "en"); useEffect(() => { localStorage.setItem("llm-wiki-language", lang); document.documentElement.lang = lang === "zh" ? "zh-CN" : "en"; }, [lang]); return <I18n.Provider value={{ lang, t: copy[lang], setLang }}><Routes><Route path="/" element={<ProjectList />} /><Route path="/projects/:projectId/*" element={<Workspace />} /><Route path="*" element={<Navigate to="/" replace />} /></Routes></I18n.Provider>; }
function LanguageToggle() { const { lang, setLang, t } = useI18n(); return <button type="button" className="language-toggle" onClick={() => setLang(lang === "en" ? "zh" : "en")} aria-label={t.language}><span className={lang === "en" ? "selected" : ""}>EN</span><i><b className={lang === "zh" ? "zh" : ""} /></i><span className={lang === "zh" ? "selected" : ""}>中</span></button>; }

function ProjectList() { const { t } = useI18n(); const [projects, setProjects] = useState<Project[]>([]), [name, setName] = useState(""), [error, setError] = useState(""), [busy, setBusy] = useState(true), [selected, setSelected] = useState<string[]>([]); const navigate = useNavigate(); const load = () => api.projects().then(setProjects).catch(e => setError(e.message)).finally(() => setBusy(false)); useEffect(() => { void load(); }, []); const create = async (event: FormEvent) => { event.preventDefault(); if (!name.trim()) return; try { const project = await api.createProject(name); navigate(`/projects/${project.id}`); } catch (e) { setError((e as Error).message); } }; const remove = async () => { if (!selected.length) return; try { await api.deleteProjects(selected); setSelected([]); await load(); } catch (e) { setError((e as Error).message); } }; return <main className="landing"><header className="brand"><span className="brand-mark">✦</span>{t.brand}<LanguageToggle /></header><section className="hero"><p className="eyebrow">{t.eyebrow}</p><h1>{t.hero}</h1><p className="lede">{t.lede}</p><form className="create-card" onSubmit={create}><label htmlFor="project-name">{t.start}</label><div><input id="project-name" value={name} onChange={e => setName(e.target.value)} placeholder={t.example} /><button>{t.create} <span>→</span></button></div></form>{error && <Alert message={error} />}</section><section className="project-section"><div className="section-title"><h2>{t.projects}</h2><div className="project-actions"><span>{projects.length} {t.total}</span><button className="button danger" disabled={!selected.length} onClick={remove}>{t.delete} ({selected.length})</button></div></div>{busy ? <p className="muted">{t.loading}</p> : projects.length ? <div className="project-grid">{projects.map(project => <div className="project-card" key={project.id}><input className="project-check" type="checkbox" checked={selected.includes(project.id)} onChange={() => setSelected(current => current.includes(project.id) ? current.filter(id => id !== project.id) : [...current, project.id])} /><Link to={`/projects/${project.id}`}><div className="project-icon">◈</div><h3>{project.name}</h3><p>{project.profileConfirmed ? t.active : project.profile ? t.review : t.setup}</p><small>{t.created} {new Date(project.createdAt).toLocaleDateString()}</small><span className="arrow">→</span></Link></div>)}</div> : <div className="empty-state">{t.empty}</div>}</section></main>; }function Workspace() { const { projectId = "" } = useParams(); const { t } = useI18n(); const [snapshot, setSnapshot] = useState<ProjectSnapshot>(); const [error, setError] = useState(""); const load = useCallback(() => api.snapshot(projectId).then(setSnapshot).catch(e => setError(e.message)), [projectId]); useEffect(() => { void load(); }, [load]); useEffect(() => { if (!snapshot?.job || ["completed", "failed"].includes(snapshot.job.status)) return; const timer = window.setInterval(() => void load(), 1800); return () => window.clearInterval(timer); }, [snapshot?.job, load]); if (error) return <main className="page"><Alert message={error} /></main>; if (!snapshot) return <main className="page muted">{t.opening}</main>; return <div className="workspace"><aside className="sidebar"><Link className="logo" to="/"><span>✦</span>{t.brand}</Link><div className="project-name">{snapshot.project.name}</div><nav><NavLink to={`/projects/${projectId}`} end>{t.overview}</NavLink><NavLink to={`/projects/${projectId}/graph`}>{t.graph} <b>{snapshot.nodes.length}</b></NavLink><NavLink to={`/projects/${projectId}/search`}>{t.search}</NavLink></nav><div className="sidebar-foot">{t.evidenceLabel}-first research<br />Local MVP · v0.1</div></aside><main className="workspace-main"><Routes><Route index element={<Overview snapshot={snapshot} reload={load} />} /><Route path="graph" element={<GraphView snapshot={snapshot} />} /><Route path="search" element={<SearchView snapshot={snapshot} />} /></Routes></main></div>; }
function Overview({ snapshot, reload }: { snapshot: ProjectSnapshot; reload: () => void }) { const { t } = useI18n(); const { project } = snapshot; const [profile, setProfile] = useState<WikiProfile>(project.profile ?? emptyProfile); const [saving, setSaving] = useState(false), [error, setError] = useState(""), [selected, setSelected] = useState<string[]>([]); useEffect(() => { setProfile(project.profile ?? emptyProfile); setSelected(previous => previous.filter(id => snapshot.documents.some(d => d.id === id))); }, [project.profile, snapshot.documents]); const update = (key: keyof WikiProfile, value: WikiProfile[keyof WikiProfile]) => setProfile(previous => ({ ...previous, [key]: value })); const upload = async (event: ChangeEvent<HTMLInputElement>, role: "profile" | "source") => { const files = event.target.files; if (!files?.length) return; try { for (const file of Array.from(files)) await api.upload(project.id, file, role); if (role === "profile") await api.understandProfile(project.id); await reload(); } catch (e) { setError((e as Error).message); } finally { event.target.value = ""; } }; const save = async () => { setSaving(true); try { await api.updateProfile(project.id, profile); await reload(); } catch (e) { setError((e as Error).message); } finally { setSaving(false); } }; const confirm = async () => { setSaving(true); try { await api.confirm(project.id); await reload(); } catch (e) { setError((e as Error).message); } finally { setSaving(false); } }; const remove = async () => { if (!selected.length) return; setSaving(true); try { await api.deleteDocuments(project.id, selected); setSelected([]); await reload(); } catch (e) { setError((e as Error).message); } finally { setSaving(false); } }; const toggle = (id: string) => setSelected(current => current.includes(id) ? current.filter(value => value !== id) : [...current, id]); return <><PageHeader title={t.workspace} subtitle={t.workspaceSub} actions={<><a className="button ghost" href={api.exportUrl(project.id, "json")}>{t.exportJson}</a><a className="button ghost" href={api.exportUrl(project.id, "csv")}>{t.exportCsv}</a></>} />{error && <Alert message={error} />}<div className="overview-grid"><section className="panel profile-panel"><div className="panel-heading"><div><p className="eyebrow">01 · {t.profile}</p><h2>{t.guide}</h2></div><Status label={project.profileConfirmed ? t.confirmed : project.profile ? t.ready : t.required} tone={project.profileConfirmed ? "good" : "pending"} /></div><p className="help">{t.help}</p><Field label={t.objective}><textarea value={profile.researchGoal} onChange={e => update("researchGoal", e.target.value)} placeholder={t.objectiveHint} /></Field><div className="field-row"><Field label={t.domain}><input value={profile.domain} onChange={e => update("domain", e.target.value)} /></Field><Field label={t.entityTypes}><input value={join(profile.entityTypes)} onChange={e => update("entityTypes", split(e.target.value))} placeholder={t.entitiesHint} /></Field></div><div className="field-row"><Field label={t.relations}><input value={join(profile.preferredRelations)} onChange={e => update("preferredRelations", split(e.target.value))} placeholder={t.relationsHint} /></Field><Field label={t.ignore}><input value={join(profile.exclude)} onChange={e => update("exclude", split(e.target.value))} placeholder={t.ignoreHint} /></Field></div><Field label={t.preferences}><textarea value={profile.notes} onChange={e => update("notes", e.target.value)} placeholder={t.preferencesHint} /></Field><div className="toggles"><Toggle label={t.numeric} checked={profile.extractNumericData} onChange={v => update("extractNumericData", v)} /><Toggle label={t.units} checked={profile.preserveUnits} onChange={v => update("preserveUnits", v)} /><Toggle label={t.evidence} checked={profile.evidenceRequired} onChange={v => update("evidenceRequired", v)} /></div><div className="actions"><button className="button ghost" disabled={saving} onClick={save}>{t.save}</button><button className="button primary" disabled={saving || !profile.researchGoal.trim()} onClick={confirm}>{saving ? t.working : project.profileConfirmed ? t.reprocess : t.confirm} <span>→</span></button></div></section><section className="right-stack"><section className="panel upload-panel"><p className="eyebrow">02 · {t.sources}</p><h2>{t.upload}</h2><UploadBox label={t.profile} detail={t.profileDetail} accept=".docx" onChange={e => void upload(e, "profile")} /><UploadBox label={t.sources} detail={t.sourceDetail} accept=".pdf,.docx" multiple onChange={e => void upload(e, "source")} /><div className="document-actions"><span>{selected.length} {t.selected}</span><button className="button danger" disabled={!selected.length || saving} onClick={remove}>{t.delete}</button></div><p className="selection-help">{t.deleteHelp}</p><div className="document-list">{snapshot.documents.length ? snapshot.documents.map(d => <label className="document" key={d.id}><input type="checkbox" checked={selected.includes(d.id)} onChange={() => toggle(d.id)} /><span className="doc-icon">{d.kind.toUpperCase()}</span><div><strong>{d.fileName}</strong><small>{d.role === "profile" ? t.profileRole : t.source} · {d.status}</small></div></label>) : <p className="muted">{t.noFiles}</p>}</div></section><JobPanel snapshot={snapshot} /></section></div></>; }function highlightEvidence(text: string, term: string): string {
  if (!text) return "";
  const escaped = text.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
  if (!term) return escaped;
  const tokens = term.split(/\s+/).filter(Boolean).map(t => t.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"));
  if (!tokens.length) return escaped;
  const pattern = tokens.join("|");
  return escaped.replace(new RegExp(`(${pattern})`, "gi"), "<mark>$1</mark>");
}
function GraphView({ snapshot }: { snapshot: ProjectSnapshot }) {
  const { t } = useI18n();
  const [selected, setSelected] = useState<WikiNode | undefined>(snapshot.nodes[0]);
  const [query, setQuery] = useState(""), [type, setType] = useState("all");
  const [zoom, setZoom] = useState(1), [pan, setPan] = useState({ x: 0, y: 0 }), [drag, setDrag] = useState<{ x: number; y: number }>();
  const types = useMemo(() => [...new Set(snapshot.nodes.map(node => node.type))], [snapshot.nodes]);
  const nodes = useMemo(() => snapshot.nodes.filter(n => (type === "all" || n.type === type) && `${n.displayName} ${n.summary}`.toLowerCase().includes(query.toLowerCase())), [snapshot.nodes, type, query]);
  const shown = useMemo(() => new Set(nodes.map(n => n.id)), [nodes]);
  const edges = useMemo(() => snapshot.edges.filter(e => shown.has(e.sourceNodeId) && shown.has(e.targetNodeId)), [snapshot.edges, shown]);
  const positions = useMemo(() => layoutNodes(nodes, edges), [nodes, edges]);
  const evidence = selected ? snapshot.evidence.filter(item => selected.evidenceIds.includes(item.id)) : [];
  const docNames = useMemo(() => new Map(snapshot.documents.map(d => [d.id, d.fileName])), [snapshot.documents]);
  const related = selected ? snapshot.nodes.filter(n => n.id !== selected.id && snapshot.edges.some(e => (e.sourceNodeId === selected.id && e.targetNodeId === n.id) || (e.targetNodeId === selected.id && e.sourceNodeId === n.id))) : [];
  const transform = `translate(${pan.x} ${pan.y}) scale(${zoom})`;
  const canvasRef = useRef<HTMLDivElement>(null);
  useEffect(() => {
    const element = canvasRef.current;
    if (!element) return;
    const onWheel = (event: WheelEvent) => { event.preventDefault(); setZoom(value => Math.max(.1, Math.min(20, value * (event.deltaY < 0 ? 1.12 : .89)))); };
    element.addEventListener("wheel", onWheel, { passive: false });
    return () => element.removeEventListener("wheel", onWheel);
  }, []);
  return <><PageHeader title={t.graph} subtitle={t.graphSub} actions={<Link className="button ghost" to={`/projects/${snapshot.project.id}/search`}>{t.searchKnowledge}</Link>} /><div className="graph-toolbar"><input aria-label={t.find} value={query} onChange={e => setQuery(e.target.value)} placeholder={t.find} /><select value={type} onChange={e => setType(e.target.value)}><option value="all">{t.allTypes}</option>{types.map(value => <option key={value}>{value}</option>)}</select><button className="button" onClick={() => { setZoom(1); setPan({ x: 0, y: 0 }); }}>Fit</button><span>{nodes.length} {t.nodes} · {edges.length} {t.relationsCount}</span></div>{!nodes.length ? <div className="empty-state graph-empty">{t.graphEmpty}</div> : <div className="graph-layout"><section className="graph-canvas" ref={canvasRef}><div className="graph-hint">滚轮缩放 · 拖拽平移 · 点击节点查看证据</div><svg viewBox="0 0 1100 760" onPointerDown={e => setDrag({ x: e.clientX, y: e.clientY })} onPointerMove={e => { if (drag) { setPan(value => ({ x: value.x + e.clientX - drag.x, y: value.y + e.clientY - drag.y })); setDrag({ x: e.clientX, y: e.clientY }); } }} onPointerUp={() => setDrag(undefined)} onPointerLeave={() => setDrag(undefined)}><g transform={transform}>{edges.map(edge => { const a = positions.get(edge.sourceNodeId), b = positions.get(edge.targetNodeId); return a && b ? <line key={edge.id} x1={a.x} y1={a.y} x2={b.x} y2={b.y} className={edge.relationStatus === "inferred" ? "graph-line inferred" : "graph-line"} /> : null; })}{nodes.map(node => { const p = positions.get(node.id)!; const active = selected?.id === node.id; const radius = 6 + node.importance * 6; return <g key={node.id} className="graph-node" transform={`translate(${p.x} ${p.y})`} onClick={e => { e.stopPropagation(); setSelected(node); }}><circle r={active ? radius + 3 : radius} className={active ? "active" : ""} />{zoom >= .6 && <text x={radius + 4} y="3">{node.displayName.slice(0, 24)}</text>}</g>; })}</g></svg></section><aside className="node-detail">{selected ? <><p className="eyebrow">{selected.type}</p><h2>{selected.displayName}</h2><p>{selected.summary || t.noSummary}</p><Metric label={t.confidence} value={`${Math.round(selected.confidence * 100)}%`} /><Metric label={t.importance} value={`${Math.round(selected.importance * 100)}%`} /><h3>{t.properties}</h3>{Object.keys(selected.properties).length ? <dl>{Object.entries(selected.properties).map(([key, value]) => <div key={key}><dt>{key}</dt><dd>{String(value)}</dd></div>)}</dl> : <p className="muted">{t.noProperties}</p>}<h3>{t.related}</h3>{related.length ? <div className="related-list">{related.map(n => <button key={n.id} className="related-chip" onClick={() => setSelected(n)}>{n.displayName}</button>)}</div> : <p className="muted">{t.noProperties}</p>}<h3>{t.evidenceLabel}</h3>{evidence.length ? evidence.map(item => <article className="evidence" key={item.id}><header><span className="evidence-src">{docNames.get(item.documentId) ?? "source"}</span><span className="evidence-page">p. {item.page}</span><span className="evidence-status">{item.status}</span></header><p dangerouslySetInnerHTML={{ __html: highlightEvidence(item.originalText, selected.displayName) }} /><small>{item.section ?? item.blockId}</small></article>) : <p className="muted">{t.noProperties}</p>}</> : <p className="muted">{t.selectNode}</p>}</aside></div>}</>;
}
function layoutNodes(nodes: WikiNode[], edges: ProjectSnapshot["edges"]) {
  const count = nodes.length;
  const positions = new Map<string, { x: number; y: number }>();
  if (!count) return positions;
  const center = { x: 550, y: 380 };
  if (count === 1) { positions.set(nodes[0].id, center); return positions; }
  const radii = new Map(nodes.map(node => [node.id, 6 + node.importance * 6]));
  nodes.forEach((node, index) => {
    const seed = [...node.id].reduce((sum, char) => (sum * 31 + char.charCodeAt(0)) >>> 0, 0);
    const angle = (Math.PI * 2 * index) / count + (seed % 100) / 1000;
    const distance = Math.min(190, 70 + Math.sqrt(count) * 22) + (seed % 38);
    positions.set(node.id, { x: center.x + Math.cos(angle) * distance, y: center.y + Math.sin(angle) * distance });
  });
  const idealDistance = Math.max(48, Math.min(125, Math.sqrt((980 * 690) / count) * .62));
  let temperature = Math.min(40, 12 + Math.sqrt(count) * 6);
  for (let iteration = 0; iteration < 200; iteration += 1) {
    const displacement = new Map(nodes.map(node => [node.id, { x: 0, y: 0 }]));
    for (let a = 0; a < count; a += 1) for (let b = a + 1; b < count; b += 1) {
      const first = nodes[a], second = nodes[b];
      const fp = positions.get(first.id)!, sp = positions.get(second.id)!;
      let dx = fp.x - sp.x, dy = fp.y - sp.y, distance = Math.hypot(dx, dy);
      if (distance < .01) { dx = 1; dy = 0; distance = 1; }
      const minGap = radii.get(first.id)! + radii.get(second.id)! + 8;
      const force = (idealDistance * idealDistance) / Math.max(distance - minGap, 1);
      const fd = displacement.get(first.id)!, sd = displacement.get(second.id)!;
      fd.x += dx / distance * force; fd.y += dy / distance * force;
      sd.x -= dx / distance * force; sd.y -= dy / distance * force;
    }
    edges.forEach(edge => {
      const source = positions.get(edge.sourceNodeId), target = positions.get(edge.targetNodeId);
      const sd = displacement.get(edge.sourceNodeId), td = displacement.get(edge.targetNodeId);
      if (!source || !target || !sd || !td) return;
      const dx = target.x - source.x, dy = target.y - source.y, distance = Math.max(Math.hypot(dx, dy), 1);
      const desired = idealDistance + radii.get(edge.sourceNodeId)! + radii.get(edge.targetNodeId)!;
      const force = Math.max(0, distance - desired) * .08;
      sd.x += dx / distance * force; sd.y += dy / distance * force;
      td.x -= dx / distance * force; td.y -= dy / distance * force;
    });
    nodes.forEach(node => {
      const position = positions.get(node.id)!, delta = displacement.get(node.id)!;
      delta.x += (center.x - position.x) * .012; delta.y += (center.y - position.y) * .012;
      const magnitude = Math.max(Math.hypot(delta.x, delta.y), 1);
      const move = Math.min(magnitude, temperature);
      position.x += delta.x / magnitude * move;
      position.y += delta.y / magnitude * move;
    });
    temperature *= .96;
  }
  for (let pass = 0; pass < 10; pass += 1) {
    let moved = false;
    for (let a = 0; a < count; a += 1) for (let b = a + 1; b < count; b += 1) {
      const fp = positions.get(nodes[a].id)!, sp = positions.get(nodes[b].id)!;
      const dx = sp.x - fp.x, dy = sp.y - fp.y;
      const dist = Math.max(Math.hypot(dx, dy), .01);
      const minGap = radii.get(nodes[a].id)! + radii.get(nodes[b].id)! + 6;
      if (dist < minGap) {
        const overlap = (minGap - dist) / 2;
        const ux = dx / dist, uy = dy / dist;
        fp.x -= ux * overlap; fp.y -= uy * overlap;
        sp.x += ux * overlap; sp.y += uy * overlap;
        moved = true;
      }
    }
    if (!moved) break;
  }
  return positions;
}
function SearchView({ snapshot }: { snapshot: ProjectSnapshot }) { const { t } = useI18n(); const [query, setQuery] = useState(""), [results, setResults] = useState<WikiNode[]>(snapshot.nodes); const search = async (value: string) => { setQuery(value); setResults((await api.search(snapshot.project.id, value)).nodes); }; return <><PageHeader title={t.searchKnowledge} subtitle={t.searchSub} actions={<a className="button ghost" href={api.exportUrl(snapshot.project.id, "csv")}>{t.exportCsv}</a>} /><section className="search-panel"><input autoFocus className="search-input" value={query} onChange={e => void search(e.target.value)} placeholder={t.searchHint} /><p className="muted">{results.length} {t.matching}</p><div className="search-results">{results.map(node => <article key={node.id}><div><span className="type-pill">{node.type}</span><h2>{node.displayName}</h2><p>{node.summary || t.noSummary}</p>{node.aliases.length > 0 && <small>{t.alsoKnown}: {node.aliases.join(", ")}</small>}</div><div className="score">{Math.round(node.confidence * 100)}<small>{t.confidence}</small></div></article>)}{!results.length && <div className="empty-state">{t.noResults}</div>}</div></section></>; }
function PageHeader({ title, subtitle, actions }: { title: string; subtitle: string; actions?: React.ReactNode }) { return <header className="page-header"><div><h1>{title}</h1><p>{subtitle}</p></div><div className="header-actions">{actions}<LanguageToggle /></div></header>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="field"><span>{label}</span>{children}</label>; }
function Toggle({ label, checked, onChange }: { label: string; checked: boolean; onChange: (next: boolean) => void }) { return <label className="toggle"><input type="checkbox" checked={checked} onChange={e => onChange(e.target.checked)} /><span />{label}</label>; }
function UploadBox(props: { label: string; detail: string; accept: string; multiple?: boolean; onChange: (event: ChangeEvent<HTMLInputElement>) => void }) { const { t } = useI18n(); return <label className="upload-box"><input type="file" accept={props.accept} multiple={props.multiple} onChange={props.onChange} /><span className="upload-icon">↑</span><strong>{props.label}</strong><small>{props.detail}</small><em>{props.multiple ? t.chooseMany : t.choose}</em></label>; }
function Status({ label, tone }: { label: string; tone: "good" | "pending" }) { return <span className={`status ${tone}`}>{label}</span>; }
function Alert({ message }: { message: string }) { return <div className="alert" role="alert">{message}</div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="metric"><span>{label}</span><strong>{value}</strong></div>; }
function JobPanel({ snapshot }: { snapshot: ProjectSnapshot }) { const { t } = useI18n(); const job = snapshot.job; return <section className="panel job-panel"><p className="eyebrow">03 · {t.processing}</p><h2>{t.processing}</h2>{job ? <><div className="job-head"><Status label={job.status.replace("_", " ")} tone={job.status === "completed" ? "good" : "pending"} /><strong>{job.progress}%</strong></div><div className="progress"><i style={{ width: `${job.progress}%` }} /></div><p>{job.message}</p>{job.errors.map(error => <Alert key={error} message={error} />)}</> : <p className="muted">{t.processingHelp}</p>}</section>; }
function position(index: number, total: number) { const a = (Math.PI * 2 * index) / Math.max(total, 1) - Math.PI / 2, r = Math.min(180, 58 + total * 13); return { x: 400 + Math.cos(a) * r, y: 260 + Math.sin(a) * r }; }
