/** Compatibility entry point for the job pipeline; implementation lives in the raw parsing layer. */
export { parseDocument, parseDocumentFile, parseDocx, parsePdf, DocumentParseError } from "../document-parser.js";
